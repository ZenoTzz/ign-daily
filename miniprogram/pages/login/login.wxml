<view class="page login-page">
  <view class="brand">
    <view class="mark">IGN</view>
    <view>
      <view class="title">IGN Daily</view>
      <view class="subtitle">微信管理员登录</view>
    </view>
  </view>

  <view class="wechat-status"><view class="wechat-dot {{bindingRequired ? 'pending' : ''}}"></view><text>{{statusText}}</text></view>

  <view class="panel login-panel" wx:if="{{!checkingWechat}}">
    <input class="input" value="{{username}}" placeholder="用户名" placeholder-class="placeholder"
      bindinput="onUsernameInput" />
    <input class="input" value="{{password}}" password placeholder="密码" placeholder-class="placeholder"
      bindinput="onPasswordInput" confirm-type="done" bindconfirm="submit" />
    <button class="btn btn-primary login-btn" loading="{{loading}}" disabled="{{loading}}" bindtap="submit">
      {{bindingRequired ? '验证并绑定此微信' : '服务器账号登录'}}
    </button>
    <button class="retry-btn" bindtap="tryWechatLogin">重新检测微信身份</button>
    <view class="hint muted">首次绑定需要验证一次网站管理员账号。绑定成功后，此微信号可直接登录，不保存密码。不要填写服务器 SSH 密码。</view>
  </view>
</view>
